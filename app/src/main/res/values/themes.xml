<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FirstSubmission" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorSurfaceContainer">@color/blue</item>
        <item name="colorPrimary">@color/blue</item>
        <item name="titleTextColor">@color/white</item>
        <item name="android:colorBackground">@color/white</item>
        <item name="android:windowSplashScreenBackground">@android:color/background_light</item>
    </style>

    <style name="Theme.FirstSubmission" parent="Base.Theme.FirstSubmission" />
</resources>